import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    h1: { color: "#000000", fontWeight: "bolder", fontSize: "24em" },
    body1: { color: "black[300]", fontWeight: "lighter", fontSize: "15em" },
  },
});
